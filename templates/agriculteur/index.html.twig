{% extends 'base.html.twig' %}

{% block title %}Agriculteur index{% endblock %}
{% block listType %}
    {% for typeProduit in typeProduits %}
        <a href="{{ path('app_produit_show', {'type_id': typeProduit.id}) }}">{{ typeProduit.libelle }}</a>
    {% endfor %}
{% endblock %}
{% block body %}
    <h1>Vos agriculteurs à proximité</h1>
    <table class="table">
        <!-- <thead>
            <tr>
                <th style="color: #0a0a0a">Name</th>
                <th style="color: #0a0a0a">Lastname</th>
                <th style="color: #0a0a0a">actions</th>
            </tr>
        </thead>-->
        <tbody>
            <tr>
                {% for agriculteur in agriculteurs %}
                <th style="text-align: center; color: #0a0a0a; border-radius: 20%">
                    <a href="{{ path('app_agriculteur_show', {'id': agriculteur.id}) }}">
                        <img style="width: 20%;" src="{{ asset('img/profile.png') }}" alt="profil"><br>
                        {{ agriculteur.lastname }}
                    </a>
                </th>
                {% endfor %}
            </tr>
            <!-- <tr>
                <td colspan="6">no records found</td>
            </tr>-->
        </tbody>
    </table>

    <br>
    <img src="{{ asset('img/map.png') }}" alt="Map world" style="
                     display: block;
                     margin-left: auto;
                     margin-right: auto;">
    <br>
{% endblock %}